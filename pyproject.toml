[project]
name = "programming-for-da"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alan0220", email = "alansaji030@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.9.4",
    "pandas>=2.3.3",
    "seaborn>=0.13.2",
]

[project.scripts]
programming-for-da = "programming_for_da:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
